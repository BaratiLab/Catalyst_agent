[project]
name = "structure-retrieval-server"
version = "0.1.0"
description = "MCP server for OPTIMADE structure search; writes results.json and results_short.json"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.18.0",
    "pandas",
    "pymatgen>=2025.10.7",
]
